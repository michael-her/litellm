# Copy to cursor.local.env and set your key (Dashboard → Integrations).
CURSOR_API_KEY=
COORD_WAKE_MODEL=composer-2.5
# Optional: default workspace for Cursor SDK local agents
# CURSOR_CWD=G:/dev/litellm
# Optional: attach to an existing Cursor bridge instead of launching one
# CURSOR_SDK_BRIDGE_URL=http://127.0.0.1:PORT
# CURSOR_SDK_BRIDGE_TOKEN=
